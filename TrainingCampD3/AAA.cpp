#include <bits/stdc++.h>

using namespace std;

#define MEM(a, b) menset(a, (b), sizeof(a))
#define forn(i, n) for(int i = 0; i < n; i++)
#define PB push_back

typedef pair<int, int> PII;
typedef vector<int> VI;
